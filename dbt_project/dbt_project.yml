name: "dwh_risk"
version: "1.0.0"
config-version: 2

profile: "dwh_risk"

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  dwh_risk:
    staging:
      +schema: stg
      +materialized: view
    marts:
      +schema: mart
      +materialized: table
